# TOOL schemas

Traceability and executable-view schemas owned by TOOL. Implementation-profile
and dependency-policy schemas belong to IMPL.

- `traceability.schema.toml` — schema 1.3 stores
  canonical artifact and commit relations without authored inverse edges
