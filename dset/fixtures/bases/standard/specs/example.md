# Example delta

## ADDED — FEATURE-REQ-001 Helpful output

The fixture must produce an exact envelope with human-usable content.

**Scenario FEATURE-SCN-001:** Given a valid input, the envelope is exact and a reviewer rates the message useful.
