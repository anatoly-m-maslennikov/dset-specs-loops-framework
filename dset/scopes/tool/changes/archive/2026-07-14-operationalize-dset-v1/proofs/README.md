# DSET v1 proof evidence

Store bounded deterministic summaries, fixture results, skill validation,
qualitative evals, and pushed-head archive-readiness evidence here. Each
promoted proof records LLM session IDs or explicit `none` for human-only work.
Keep raw transient logs outside committed truth unless a concise redacted
excerpt is required to explain a failure.
