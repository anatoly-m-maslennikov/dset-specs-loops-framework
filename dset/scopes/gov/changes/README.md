# GOV Changes

Each direct child except `archive/` is one active GOV-owned Change, identified by a stable `DSET-CHANGE-GOV-NNN` ID and a readable directory slug.

No GOV Change is currently active. See the [archive](archive/README.md).
