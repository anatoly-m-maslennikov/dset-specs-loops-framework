# Schemas

This directory will contain released, machine-readable DSET contracts. The first schema set must validate the project manifest, package specs, active change structure, artifact IDs, test/eval separation, archive readiness, and traceability references.

`dset/dset.yaml` is currently marked `0.1-draft`; it must not be advertised as a stable external contract until the schema and fixtures exist.
