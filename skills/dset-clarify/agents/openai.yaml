interface:
  display_name: "DSET Clarify"
  short_description: "Turn ambiguity into accepted domain decisions"
  default_prompt: "Use $dset-clarify to clarify this change before writing its specification."
