# Tasks — Make DSET self-hosting and skills thin

- [x] **DSET-TASK-META-006:** Refine all nine roadmap invariants for profile applicability, justified non-applicability, and rule-ID ownership.
- [x] **DSET-TASK-META-007:** Add one candidate requirement/scenario and deterministic test mapping per invariant plus separate qualitative evals.
- [x] **DSET-TASK-META-008:** Reconcile the invariant contract into candidate accepted methodology truth and package registration.
- [x] **DSET-TASK-GOV-011:** Run the current structural checks, record bounded evidence, and confirm no planned 0.3 mechanics are claimed as implemented.
- [x] **DSET-TASK-OPS-015:** Implement roadmap §0 version and bootstrap semantics.
- [x] **DSET-TASK-GOV-012:** Implement roadmap §1 repository-local governance registry and resolver.
- [x] **DSET-TASK-SKILL-002:** Implement roadmap §2 thin skills and record the former-rule ownership inventory.
- [x] **DSET-TASK-TOOL-006:** Implement roadmap §3 project-owned materialization, customization, migration, and update comparison.
- [x] **DSET-TASK-TOOL-007:** Complete roadmap §4 hosted fixed-point proof after publishing the draft PR.
- [ ] **DSET-TASK-TOOL-008:** Run the complete DSET 0.4 deterministic release plan and independent qualitative evals after the remaining DSET-only host, platform, and hosted gates are complete. External adopter profiles and pilots are excluded by `DSET-VERSION-002`.
- [ ] **DSET-TASK-TOOL-009:** Reconcile the bounded DSET 0.4 Change, emit its Readiness and Release records, and satisfy archive readiness after the remaining DSET-only gates pass. Historical roadmap §§5–§7 belong to their external projects and are not prerequisites for this Change.
- [x] **DSET-TASK-META-009:** Finalize the five-skill core topology, primary orchestration boundary, bounded run-record contract, version transition matrix, release transaction, and RC/final readiness requirements with separate tests and evals.
- [x] **DSET-TASK-SKILL-003:** Define same-model/same-effort delegation inheritance, medium useful fan-out, explicit deviations, outcome-cost-aware model selection, and low/medium/high budget proof obligations.
- [x] **DSET-TASK-GOV-013:** Define the initial problem/opportunity/question intake, keep tasks inside Changes, and record the Action entity as project Question `DSET-QUESTION-GOV-001`; its taxonomy is superseded by `DSET-DECISION-GOV-007`.
- [x] **DSET-TASK-META-010:** Run three independent high-effort specification reviews, reconcile every blocking finding, and record the bounded review evidence. All critical/high findings against `ca3faa27d8c3a2beac679e053140a07b4d56f41d` are resolved and reconciled in `proofs/DSET-EVIDENCE-RECORD-019-independent-review-remediation.md`.
- [x] **DSET-TASK-TOOL-010:** Implement the runtime adapter behind `dset` and `dset-release`, local run/checkpoint records, rootless packaged initialization, release classification/version checks, synchronized version surfaces, and guarded post-merge publication automation. Local implementation and clean-wheel proof pass at `c7c0038a19ee2b95bd9d51d2d903f130aca3166e`; native-host and hosted-publication evidence remain separately owned by `DSET-TASK-SKILL-020` and `DSET-TASK-OPS-025`.
- [ ] **DSET-TASK-SKILL-020:** Complete real host-native installation, discovery, load, invocation, handoff, and stop proof for every declared agent host under `DSET-CONTRACT-SKILL-001`. At exact commit `b09cd0fe2beb7e14326c0f8bcb3693c032afada0`, repository-scoped Codex and Claude installation and verification passed for all 17 wrappers and their shared runtimes. Fresh host processes still stopped before inference: Codex could not initialize its app-owned read-only state database, and Claude Code 2.1.205 returned `403 Domain not in allowlist`. Evidence record 039 preserves this current boundary; no actual discovery/load/invocation/handoff/stop receipt is promoted.
- [x] **DSET-TASK-SKILL-021:** Implement, register, materialize, and deterministically verify all five repository-native source skill packages with one skill-owning subagent per package and applicability-aware adopter behavior.
- [x] **DSET-TASK-SKILL-022:** Implement, register, distribute, materialize, and verify the 11 additional lifecycle-mode skill packages selected by `DSET-DECISION-SKILL-002`, preserving thin-wrapper ownership and the two bounded pre-resolution exceptions.
- [ ] **DSET-TASK-TOOL-036:** Complete declared macOS, native Windows, WSL, and Linux proof or explicitly narrow applicability before publication under `DSET-CONTRACT-TOOL-001`. GitHub Actions run `29799045908` exposed native-Windows CRLF drift in byte-sealed carriers. `DSET-DECISION-OPS-008` and `DSET-TEST-PLAN-OPS-017` now pin LF checkout bytes, and `DSET-DECISION-GOV-020` prevents generated-only refresh commits from invalidating their own views. Evidence record 041 binds the clean local fixed point at `a473c29073a9660b35f2a7f78d3d715609b063fe`; fresh hosted Linux/macOS/native-Windows execution and the explicit WSL hook remain pending.
- [x] **DSET-TASK-TOOL-037:** Implement dependency allowlist/denylist, exact registry/version/license/provenance enforcement, lockfile authority, and expiring exceptions under `DSET-CONTRACT-TOOL-002`. `dset dependencies` and the repository validation gate enforce the versioned policy and exact `uv.lock` graph.
- [ ] **DSET-TASK-OPS-025:** Capture real GitHub Actions run/check, protected-branch, and exact-merge-SHA release-publisher evidence for the actual implementing PR under `DSET-CONTRACT-OPS-001`. Source workflows and deterministic collision/idempotency tests pass; real publication requires a version-changing `dev` to `main` delivery.
- [x] **DSET-TASK-META-015:** Define Outcome as a measurable state change and record the evidence-backed DSET 0.3 adoption-readiness Outcome.
- [x] **DSET-TASK-META-016:** Define and reconcile the Work Area entity, invariant, requirement, and Contract with separate deterministic test and qualitative eval mappings without claiming runtime enforcement.
- [x] **DSET-TASK-TOOL-038:** Implement schema 1.2 Work Area declaration validation, repository-or-area Change targets, `dset new --work-area`, and trace propagation with deterministic fixtures.
- [x] **DSET-TASK-GOV-039:** Preserve Journey, Actor/Persona, Hypothesis/Experiment, prioritization, feedback/analytics, and generated roadmap/release views as candidate 0.6 Roadmap items without creating current atomic authority or new semantic Types.
- [x] **DSET-TASK-GOV-040:** Strengthen Architecture as the governance constitution, separate dependency from precedence and authority from assurance, promote accepted local GOV rules into `core-v1`, and add schema/validator/test coverage under `DSET-DECISION-GOV-002`.
- [x] **DSET-TASK-GOV-041:** Recursively enforce and self-apply explicit LLM-session provenance across current transactional artifacts, run/checkpoint schemas, validators, templates, fixtures, and the bounded generated adopter under `DSET-DECISION-GOV-001`.
- [x] **DSET-TASK-GOV-042:** Define and compile the project-health, external-review, and universal artifact-priority requirements into accepted GOV/TOOL truth with distinct deterministic tests and qualitative evals.
- [x] **DSET-TASK-TOOL-043:** Implement the deterministic project-health model, portable Markdown renderer, explicit refresh, staleness check, repository/layer/package/Work Area drill-downs, and coverage fixture matrix. `dset health` now owns the byte-stable read/write/check flow; the qualitative usefulness Evaluation remains separate.
- [ ] **DSET-TASK-GOV-044:** Complete an authenticated Codex-to-Claude-to-DSET audit and reconciliation. The deterministic packet/report/reconciliation runtime and fixtures are implemented. Current `DSET-REVIEW-PACKET-007` binds the repaired pull-request fixed-point candidate `231c56db48a9d1805efdc42f48767e55387dbbe1`, ten reviewed inputs, resolved rules, scope, and criteria. After explicit operator approval, Claude Code 2.1.205 still stopped before inference with `403 Domain not in allowlist` and zero token/tool use; evidence record 043 preserves the exact boundary, so an independent report and explicit finding reconciliation remain pending.
- [x] **DSET-TASK-GOV-045:** Resolve the project priority scale and inheritance/override/conflict policy; define immutable atom/lifecycle/absorption/archive schemas including first-class Conflict atoms and resolution events; migrate current atomic artifacts to append-only state; add explicit or inherited priority across every governed artifact class; and propagate priority/lifecycle sources, open Conflicts, conflict outcomes, archive lookup, and unknown states into traceability and generated health views. Implemented by the bounded priority profile, immutable atom/lifecycle registries, source compilation, conflict runtime, byte-stable retirement archive, and trace/health projections; legacy carriers remain immutable and are handled by compatibility classification rather than rewritten.
- [x] **DSET-TASK-TOOL-046:** Implement deterministic role-aware conflict disposition for every governed artifact pairing, including semantic classification independent of workflow, false-Conflict rejection, active-source/projection and absorbing/absorbed lifecycle rules, immutable-authority guards, explicit unsatisfiable states, assurance/conformance/staleness routing, selection-policy/precedence checks, first-class Conflict emission, recorded resolution evidence, tie stops, and reprioritization invalidation. The executable resolver covers every governed role pair, gated Conflict atom emission, append-only resolution, digest-bound result checks, and reprioritization invalidation at `6b8aa6955fdf4c534c2f43d6e5071ccdbc2fd0f5`.
- [x] **DSET-TASK-GOV-047:** Define the prior workflow-independent Problem, Question, and Conflict semantics; emit its absorbing Decision; compile the definitions into GOV truth and public artifact rules; and map separate deterministic Test and qualitative Evaluation obligations. `DSET-DECISION-GOV-007` now absorbs that taxonomy.
- [x] **DSET-TASK-GOV-048:** Make rationale visibly recommended for Decisions and available to other atomic artifacts while keeping it optional, bounded when structured, non-authoritative, and separate from evergreen implementation-reference truth; update schemas, templates, deterministic tests, and qualitative eval criteria.
- [x] **DSET-TASK-SKILL-023:** Pin the same-skill/multiple-project boundary as `DSET-CONTRACT-SKILL-002`, strengthen local-governance isolation semantics, and add deterministic two-project plus qualitative proof obligations without moving project policy into skill wrappers.
- [x] **DSET-TASK-SKILL-024:** Replace prerequisite-gated skill invocation with project-governed entry criteria, rename `dset-decide` to `dset-decisions`, make implementation reconcile decisions and conditionally prepare proof/build plans, and prove finite progress-bounded chaining.
- [x] **DSET-TASK-GOV-049:** Implement `DSET-DECISION-GOV-008` end to end: add the four-Type/one-direct-subtype schema and recognition boundaries; preserve distinct directive/acceptance/carrier/work/evidence roles; and add lifecycle, validators, templates, fixtures, traceability, health reporting, and skill routing. The original implementation classified 233 historical carriers at `115cb22f136c5609c49ae86b54195edc85b28a05`; `DSET-DECISION-GOV-034` later required their complete migration to canonical identities without retained short aliases.
- [x] **DSET-TASK-GOV-050:** Review the flat Type/subtype model against pinned FPF A.7, A.11, A.2.9, A.10, C.32.PAD, and E.17.AUD; emit absorbing `DSET-DECISION-GOV-008`; compile the accepted boundaries into public and repository-local governance plus META/GOV specs and proof plans; add bounded provenance and deterministic regression proof without claiming the pending runtime/schema migration.
- [x] **DSET-TASK-GOV-051:** Implement `DSET-DECISION-GOV-009`: register the independent MECE artifact-type axis, add Analysis Report and its direct subtypes, validate the project-local registry and path rules, classify current templates and Release lifecycle artifacts, and record recursive self-application proof without editing immutable atoms.
- [x] **DSET-TASK-GOV-052:** Implement type-first artifact naming, root project behavior settings, default/opt-in validation, and self-application without renaming stable history.
- [x] **DSET-TASK-GOV-053:** Require and self-apply one-level-down Mermaid architecture views at the project and every enabled layer/feature/feature-group hub, with reusable templates and regression proof.
- [x] **DSET-TASK-GOV-054:** Define and self-apply narrowest-common-scope ownership so project-level artifacts own cross-child truth, Contracts, end-to-end QA, cross-cutting policy, integration, release/readiness, and cross-owner work without duplicating child detail.
- [x] **DSET-TASK-GOV-055:** Define minimal artifact inheritance with canonical `child_of`, derived `parent_to`, direct parent applicability when no child exists, and local Decision implementation or cancellation limited to its subtree.
- [x] **DSET-TASK-GOV-056:** Implement many-to-many child-owned artifact lineage, derived `parent_to` and transitive paths, deterministic validation, generated traceability edges, and recursive self-application proof.
- [x] **DSET-TASK-SKILL-057:** Implement `DSET-REQUIREMENT-SKILL-011`: package shared mechanics once per host, add deterministic explicit-target project/Work Area and governance context resolution, enforce the complete skill/workflow catalog, remove duplicated mechanical discovery from wrappers, and prove cross-project and cross-platform behavior. Deterministic implementation proof is recorded in `proofs/DSET-EVIDENCE-RECORD-005-shared-skill-runtime.md`; native authenticated host and hosted-OS evidence remain separately owned.
- [x] **DSET-TASK-GOV-058:** Implement the artifact-creation gate now owned by `DSET-REQUIREMENT-GOV-039`: add the medium-default/high-opt-in setting, material-ambiguity diagnostics, focused-question handoff, one-step broader-scope eligibility calculation, and explicit promotion authorization proof. Deterministic implementation proof is recorded in `proofs/DSET-EVIDENCE-RECORD-006-artifact-emission-gate.md`; its qualitative Evaluation remains separate.
- [x] **DSET-TASK-TOOL-059:** Repair rootless initialization for repositories with active hosted automation: detect the target state, emit applicable supportability and an explicit incomplete-runbook warning, preserve the stricter validator, and prove the generated repository validates without weakening production requirements.
- [x] **DSET-TASK-TOOL-060:** Execute the pinned read-only Claudian clean-room comparison: preview and isolate initialization, resolve project-local governance, classify documentation/tooling owners, run applicable TypeScript gates, separate project-specific fields from reusable policy, and retain the profile as a candidate.
- [x] **DSET-TASK-OPS-061:** Reconcile the OYOHA production-supportability contract and bounded safe-diagnostic slice into framework evidence without claiming installed-host recovery, rollback, or incident-to-release proof.
- [x] **DSET-TASK-TOOL-062:** Separate evidence-derived framework profile references from project-owned applied instances, fail closed on adopter fallback, expose selected role in inspection, and cover the boundary deterministically.
- [x] **DSET-TASK-GOV-063:** Replace the split release-role classifications with the primary Delivery artifact type and six direct subtypes; preserve immutable predecessor Decisions through append-only successors; migrate the current Version Scopes, Roadmap, Release Plan, and Readiness Record into one `DSET-DELIVERY-001..007` sequence; update registry, templates, compiled GOV/OPS truth, release validation, traceability, health, and deterministic coverage.
- [x] **DSET-TASK-GOV-064:** Implement `DSET-DECISION-GOV-013`: replace untyped general lineage with ten MECE forward relations, range-based evergreen projection frontiers, derived inverses, sealed `child_of` compatibility, deterministic schemas/runtime/traceability/health, and separate Test/Evaluation proof. Current deterministic evidence is recorded in `proofs/DSET-EVIDENCE-RECORD-024-legacy-relation-sealing.md`; qualitative `DSET-EVAL-PLAN-GOV-026` remains pending.
- [x] **DSET-TASK-TOOL-065:** Resolve `DSET-DEFECT-TOOL-002` by rejecting legacy `child_of` metadata at new-atom sealing while retaining read compatibility for immutable historical carriers.
- [x] **DSET-TASK-SKILL-066:** Implement the lazy-default and strict implementation-only modes now owned by `DSET-REQUIREMENT-SKILL-013` in `dset_settings.toml`, lifecycle/runtime context, thin wrappers, and deterministic proof.
- [x] **DSET-TASK-GOV-067:** Implement `DSET-REQUIREMENT-GOV-037`: verbose canonical settings, settings-manifest ownership separation, canonical TOML mutable artifacts and new Markdown frontmatter, explicit immutable/external format boundaries, and complete reference closure.
- [x] **DSET-TASK-TOOL-068:** Build, review, and run the deterministic dry-run-first TOML migration with digest mapping, collision/unsupported-value stops, idempotency, repository/adopter gates, and recovery instructions.
- [x] **DSET-TASK-TOOL-069:** Resolve `DSET-DEFECT-TOOL-003` by permitting any immutable semantic atom to originate `replacement_of` while retaining Decision-only override and all replacement lifecycle gates.
- [x] **DSET-TASK-TOOL-070:** Resolve `DSET-DEFECT-TOOL-004`: preserve sealed atoms, promoted proof, and registered legacy Decision carriers byte-for-byte through TOML migration; remove seal rebasing and prove immutable digests.
- [x] **DSET-TASK-TOOL-071:** Resolve `DSET-DEFECT-TOOL-005`: derive health compilation, QA, implementation, and evidence coverage only from validated typed relations and current lifecycle state.
- [x] **DSET-TASK-GOV-072:** Resolve `DSET-DEFECT-GOV-005`: require exactly one artifact classification or an explicit registered exclusion for every governed carrier, including Procedure path coverage.
- [x] **DSET-TASK-TOOL-073:** Resolve `DSET-DEFECT-TOOL-006`: derive conflict authority facts from repository truth and evidence-bind irreducible caller assertions.
- [x] **DSET-TASK-GOV-074:** Resolve `DSET-DEFECT-GOV-006`: add a TOML Evidence Record schema, template, validator, compatibility boundary, and focused proof.
- [x] **DSET-TASK-GOV-075:** Implement `DSET-DECISION-GOV-016`: preserve selector-sealed package YAML byte-for-byte, emit one current native `package.toml` successor per shared package, reconcile active IDs and paths, enforce reader precedence and digest-bound readiness, and prove idempotent cutover.
- [x] **DSET-TASK-GOV-076:** Implement `DSET-DECISION-GOV-017`: register every historically required YAML snapshot by exact path, digest, current TOML owner, artifact classification, and retaining carrier; preserve physical historical links, prohibit fallback ownership, and prove fail-closed idempotent cutover.
- [x] **DSET-TASK-SKILL-077:** Resolve `DSET-DEFECT-SKILL-002`: seal installed wrapper trees and runtime payloads against expected source/render identities and reject post-install mutation in verification and receipts.
- [x] **DSET-TASK-SKILL-078:** Resolve `DSET-DEFECT-SKILL-003`: render one package-local launcher for every executable DSET instruction with shell-safe host forms and no ambient-PATH fallback.
- [x] **DSET-TASK-SKILL-079:** Resolve `DSET-DEFECT-SKILL-004`: distinguish active handoff from terminal finish, preserve explicit session identity across specialist context resolution, and prove true terminal closure separately.
- [x] **DSET-TASK-TOOL-080:** Resolve `DSET-DEFECT-TOOL-007` under `DSET-DECISION-GOV-018`, `DSET-REQUIREMENT-GOV-040`, and the atom now addressed as `DSET-REQUIREMENT-GOV-062`: implement the append-only carrier-transition ledger, migrate all ten standalone DSET YAML artifacts and seventy-one Markdown YAML-frontmatter carriers to TOML, reconcile original/current seals and all references, prove semantic equivalence and rollback, refresh generated views and bootstrap, and record current evidence for `DSET-TEST-PLAN-GOV-042..043`.
- [x] **DSET-TASK-GOV-081:** Implement `DSET-DECISION-GOV-019` and `DSET-DECISION-OPS-007`: replace Delivery with Version as the primary release-lifecycle artifact Type; preserve predecessor atoms through absorption; migrate current authority, registries, templates, active carriers, IDs, settings examples, toolchain behavior, generated views, and deterministic/qualitative proof definitions; and reject partial current-state migration. Deterministic proof is recorded in `proofs/DSET-EVIDENCE-RECORD-035-version-artifact-type.md`; qualitative `DSET-EVAL-PLAN-GOV-029` remains pending.
