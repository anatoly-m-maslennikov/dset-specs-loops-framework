# Installed project-wide methodology

Own cross-layer DSET methodology. Layer rules remain in sibling `01_meta`
through `05_ops` directories. Applied project-wide artifacts live separately
under `100_project`; repository distribution licenses live outside `.dset`.
