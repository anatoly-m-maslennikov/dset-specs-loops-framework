# Public DSET version surface

This directory contains public framework edition and release projections. It
does not own the self-hosting project's Version atoms or release-control
records; `DSET-VERSIONS-HUB.md` owns that applied navigation.

Reusable release templates start at `000_dset-ops-templates-release-hub.md`.
